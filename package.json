{
  "name": "fides-credential-catalog",
  "version": "0.1.0",
  "description": "FIDES Community Credential Catalog - Discover credential definitions and schema metadata",
  "main": "dist/index.js",
  "scripts": {
    "crawl": "tsx src/crawler/index.ts",
    "build": "tsc",
    "dev": "tsx watch src/crawler/index.ts",
    "validate": "ajv validate --spec=draft2020 -c ajv-formats -s schemas/credential-catalog.schema.json -d 'community-catalogs/**/credential-catalog.json'",
    "generate-eudi-schemas": "tsx scripts/generate-eudi-schemas.ts",
    "copy-issuer-aggregated": "node scripts/copy-issuer-aggregated.js",
    "copy-rp-aggregated": "node scripts/copy-rp-aggregated.js",
    "check-links": "tsx scripts/check-links.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FIDEScommunity/fides-credential-catalog.git"
  },
  "keywords": [
    "verifiable-credentials",
    "credential-catalog",
    "schema",
    "fides"
  ],
  "author": "FIDES Community",
  "license": "Apache-2.0",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "ajv-cli": "^5.0.0",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2"
  }
}
